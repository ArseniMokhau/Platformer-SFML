#pragma once
/*
enum BatState { FlyingR = 0, FlyingL};
class Bat
{
private:
	sf::Sprite sprite1;
	sf::Texture texture1;
	sf::Clock clock1;
	sf::Clock dirClock;

	bool animationSwap;
	short state;
	sf::IntRect currentFrame;


	sf::Vector2f velocity;

	void initVariables();
	void initTexture();
	void initSprite();
	void initAnimations();
public:
	Bat();
	virtual ~Bat();
	const bool& getAnimationSwap();
	const sf::Vector2f getPosition();
	const sf::FloatRect getGlobalBounds() const;

	void setPosition1(const float posX, float posY);

	void movement(const float x, const float y);
	
	void udateMovement();
	void resetClock();
	void resetDirClock();
	void updateAnimations();
	void update();
	void render(sf::RenderTarget& target);
};
*/
